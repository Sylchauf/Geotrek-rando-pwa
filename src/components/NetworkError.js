import React from 'react'
import { Text } from 'react-native'

const NetworkError = () => {
  return (
        <Text>Can't reach the server. Verify your network connection.</Text>
  )
}

export default NetworkError
